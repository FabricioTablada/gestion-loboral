[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: toda ruta se resuelve al index para no romper al recargar.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
